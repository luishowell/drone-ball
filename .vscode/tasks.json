{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mbed build",
            "type": "shell",
            "command": "mbed",
            "args": [
                "compile", "-m", "LPC1768", "-t", "GCC_ARM", "--flash"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}